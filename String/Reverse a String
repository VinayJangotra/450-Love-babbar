https://leetcode.com/problems/reverse-string/
class Solution {
    public void reverseString(char[] s) {
        char ch;
        for(int i=0;i<s.length/2;i++){
             ch=s[i];
             s[i]=s[s.length-i-1];
             s[s.length-i-1]=ch;
        }
    }
}
// The appraoch of the problem is very simple 
In,this approach we simply use the swap method,
first we store the value of the first character and
then swap it with the last charcter
